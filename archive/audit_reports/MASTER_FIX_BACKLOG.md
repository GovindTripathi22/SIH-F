# UrbanPulse — Master Fix Backlog (Phases 1-35)

**Audit Baseline Date:** 2026-09-08  
**Priority Hierarchy:**  
- **P0**: Blocks technical credibility / Core AI or pipeline deception  
- **P1**: Major SIH weakness / Architectural gap / Feature broken in production path  
- **P2**: Quality & verification robustness / Disconnected functionality  
- **P3**: Engineering polish / Configuration / UI edge cases  

---

## Master Defect & Remediation Index

| ID | Priority | Category | Problem | Root Cause | Affected Files | Fix Strategy | Verification Test | Expected Outcome | SIH Value |
|---|---|---|---|---|---|---|---|---|---|
| **DEF-01** | **P0** | Computer Vision / AI | Generic COCO model loaded and remapped with modulo `cls_id % 6` to road defects; detects frisbees as manholes. | `cv_service.py` loaded `yolov8n.pt` (COCO 80-class) and mapped class indices arbitrarily. | `backend/app/services/cv_service.py`, `backend/yolov8n.pt`, `cv_engine/evaluate.py` | Load genuine RDD2022 trained road defect model (`models/rdd_yolov8n.pt`). Remove modulo remapping. Explicitly map true classes: `POTHOLE`, `CRACK`, `PATCH`, `MANHOLE`, `DRAINAGE`. | Test inference on real road images with true classes; verify no remapping. | Real AI credibility; zero fake detections; SIH Red-Team proof. | **Critical (10/10)** |
| **DEF-02** | **P0** | Computer Vision Evaluation | Evaluation script fakes scores via `else: tp += 1` and hardcodes 84.2% mAP in report. | `cv_engine/evaluate.py` incremented true positives even when IoU match failed. | `cv_engine/evaluate.py`, `MODEL_EVALUATION.md` | Rewrite evaluation harness to compute genuine IoU matching, Precision, Recall, F1, and empirical latency without falsified increments. | Run `python cv_engine/evaluate.py`; produce authentic `MODEL_EVALUATION.md`. | Honest, defensible AI performance benchmarks with verified accuracy. | **Critical (10/10)** |
| **DEF-03** | **P1** | GIS / Spatial Clustering | Risk of false merging opposite-carriageway or parallel-road events due to fixed 20m radius. | `MultiPassVerificationEngine.find_matching_issue` ignores vehicle heading delta and corridor vector. | `backend/app/services/spatial_clustering.py`, `backend/app/services/gps_service.py` | Add heading consistency validation (reject merge if headings differ by > 120° on divided corridors) and GPS uncertainty-weighted thresholds. | `backend/tests/test_spatial_clustering.py`, `GEO_CLUSTER_TEST_REPORT.md` | Prevents opposite lane defects from merging into one location. | **High (9/10)** |
| **DEF-04** | **P1** | Backend Analytics | `/api/v1/analytics/traffic` crashes with HTTP 500 on SQLite. | Uses PostgreSQL-specific functions `date_trunc('hour', ...)` and `extract('epoch', ...)`. | `backend/app/api/analytics.py` | Implement dialect-agnostic queries using `strftime` for SQLite and `date_trunc` for PostgreSQL; add `/analytics/overview` endpoint. | `httpx.get('/api/v1/analytics/traffic')` and `/analytics/overview` returning HTTP 200. | Real backend telemetry and analytics without database vendor lock-in. | **High (9/10)** |
| **DEF-05** | **P1** | Frontend Integration | `AnalyticsPanel.tsx` uses static `simulatedEvents` instead of backend API. | Frontend analytics component was never wired to live backend endpoints. | `src/components/AnalyticsPanel.tsx`, `src/api/client.ts` | Wire `AnalyticsPanel.tsx` to fetch `/api/v1/analytics/traffic` and `/api/v1/analytics/road-health` with graceful offline fallback. | Load analytics view in browser; verify live database telemetry. | Production path uses live database data rather than static arrays. | **High (9/10)** |
| **DEF-06** | **P1** | Closed-Loop Verification | Repair verification does not automatically certify resolution from repeated clean passes. | `work_order_service.py` only allowed manual status advancement; lacked automated clean transit confirmation. | `backend/app/services/work_order_service.py`, `backend/app/services/spatial_clustering.py`, `backend/app/api/work_orders.py` | Add clean-pass observation logging: when N consecutive clean bus passes record NO defect post-repair, automatically upgrade to `RESOLUTION_VERIFIED`. If defect re-occurs, trigger `REPAIR_FAILED`. | Unit test with 2 clean passes after repair; verify automatic resolution. | Industry differentiator: closed-loop automated verification via fleet. | **High (9/10)** |
| **DEF-07** | **P2** | Explainable Priority | `PriorityEngine` exists as a standalone service but was partially bypassed by inline scoring. | `spatial_clustering.py` used simplified Bayesian formula instead of the full `PriorityEngine.calculate_priority`. | `backend/app/services/spatial_clustering.py`, `backend/app/services/priority_engine.py` | Unify priority calculation with full explainable factors (diversity, volume, confidence, severity, age, road importance). | Test priority scoring on issues with diverse buses vs single bus. | 100% explainable priority with transparent weights and audit trail. | **Medium (8/10)** |
| **DEF-08** | **P2** | Privacy & DPDP Claims | Unqualified claim of "DPDP compliant" in API outputs and documentation. | Terminology was overly absolute without formal third-party certification. | `backend/app/services/privacy_service.py`, `PRIVACY.md`, `README.md` | Accurately qualify as "DPDP 2023 Framework Aligned (Local Edge Anonymization Active)". Ensure OpenCV face/plate blur runs on all evidence. | `test_privacy_and_health.py` and empirical redaction verification. | Zero legal/evaluator skepticism; completely honest privacy claims. | **Medium (7/10)** |
| **DEF-09** | **P2** | GPS Snapping Pipeline | `GPSService.snap_to_route_segment` exists but was not actively executed on incoming raw events. | Ingestion stored raw latitude/longitude without corridor snapping. | `backend/app/services/event_service.py`, `backend/app/services/gps_service.py` | Execute corridor validation and road snapping during event ingestion when route coordinates are available. | Test GPS snapping with noisy inputs (+15m offset); verify snapped coordinate. | Accurate GIS mapping aligned with municipal road centerlines. | **Medium (7/10)** |
| **DEF-10** | **P3** | Documentation Alignment | Unsupported performance claims in README/docs ("99.9% bandwidth savings", "95% accuracy"). | Marketing hype copied without empirical calculation. | `README.md`, `ARCHITECTURE.md`, `SIH_FINAL_READINESS_REPORT.md` | Replace theoretical claims with measured empirical data (e.g. "98.4% bandwidth reduction: 3.2 MB/s raw video vs 52 KB/s telemetry events"). | Check claim audit against empirical test results. | 100% honest documentation verified by real test outputs. | **Medium (7/10)** |
